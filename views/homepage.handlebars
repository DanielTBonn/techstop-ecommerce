{{#if 0 includeZero=true }}

{{> homepage-details }}

{{/if}}

  <section class="product-list">
      {{#each products}}
        <div class="product">
          <h2>{{name}}</h2>
          <p>Price: ${{price}}</p>
          <button>Add to Cart</button>
        </div>
      {{/each}}
    </section>