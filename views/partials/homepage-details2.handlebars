<div class="bg-white">
  <div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
    <div class="flex justify-between">
        <h2 class="text-2xl font-bold tracking-tight text-gray-900">{{products2.[0].category.category_name}}</h2>
        <a href="/category/{{products2.[0].category_id}}">Click here for more!</a>
    </div>
   
    <div class="mt-6 grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-4 xl:gap-x-8">
      
      <div class="group relative">
        <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
          <img src="{{products2.[0].image}}" class="h-full w-full object-cover object-center lg:h-full lg:w-full">
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <a href="/product/{{products2.[0].id}}">
                <span aria-hidden="true" class="absolute inset-0"></span>
                {{products2.[0].product_name}}
              </a>
            <p class="text-sm font-medium text-gray-900">${{products2.[0].price}}</p>
            </h3>
          </div>
            <button class="cart h-10 px-5 font-semibold rounded-md bg-black text-white text-sm" id="btn-add" data-id="{{products2.[0].id}}">View Product</button>
        </div>
      </div>

      <div class="group relative">
        <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
          <img src="{{products2.[1].image}}" class="h-full w-full object-cover object-center lg:h-full lg:w-full">
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <a href="/product/{{products2.[1].id}}">
                <span aria-hidden="true" class="absolute inset-0"></span>
                {{products2.[1].product_name}}
              </a>
            <p class="text-sm font-medium text-gray-900">${{products2.[1].price}}</p>
            </h3>
          </div>
            <button class="cart h-10 px-5 font-semibold rounded-md bg-black text-white text-sm" id="btn-add" data-id="{{products2.[1].id}}">View Product</button>
        </div>
      </div>

      <div class="group relative">
        <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
          <img src="{{products2.[2].image}}" class="h-full w-full object-cover object-center lg:h-full lg:w-full">
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <a href="/product/{{products2.[2].id}}">
                <span aria-hidden="true" class="absolute inset-0"></span>
                {{products2.[2].product_name}}
              </a>
            <p class="text-sm font-medium text-gray-900">${{products2.[2].price}}</p>
            </h3>
          </div>
            <button class="cart h-10 px-5 font-semibold rounded-md bg-black text-white text-sm" id="btn-add" data-id="{{products2.[2].id}}">View Product</button>
        </div>
      </div>

      <div class="group relative">
        <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
          <img src="{{products2.[3].image}}" class="h-full w-full object-cover object-center lg:h-full lg:w-full">
        </div>
        <div class="mt-4 flex justify-between">
          <div>
            <h3 class="text-sm text-gray-700">
              <a href="/product/{{products2.[3].id}}">
                <span aria-hidden="true" class="absolute inset-0"></span>
                {{products2.[3].product_name}}
              </a>
            <p class="text-sm font-medium text-gray-900">${{products2.[3].price}}</p>
            </h3>
          </div>
            <button class="cart h-10 px-5 font-semibold rounded-md bg-black text-white text-sm" id="btn-add" data-id="{{products2.[3].id}}">View Product</button>
        </div>
      </div>

      <!-- More products... -->
    </div>
  </div>
</div>